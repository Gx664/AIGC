# 仓库文件说明

> 这份文件逐个说明仓库里**每个上传文件是干什么的**、哪个能用、哪个不能删。
> 面向两类人：想看懂项目结构的人，以及 clone 下来不知道怎么跑的人。

<p align="center"><b>中文</b> | <a href="REPO_FILES.md">English</a></p>

## 一、先看三个入口

| 你想做的事 | 该看哪个文件 |
|---|---|
| **安装并使用软件** | 下载 [`AIGC_Toolkit_Setup.exe`](AIGC_Toolkit_Setup.exe)（在仓库根目录）双击即可，其他文件都不用管 |
| 了解功能和用法 | [`README.md`](README.md) |
| 看每个版本改了什么 | [`CHANGELOG.md`](CHANGELOG.md) |

⚠️ **普通用户只需要 `AIGC_Toolkit_Setup.exe` 一个文件**（就在本仓库最上层）。其他都是给开发者看的。

---

## 二、根目录文件（含安装器）

| 文件 | 用途 |
|---|---|
| **`AIGC_Toolkit_Setup.exe`（23.6 MB）** | **安装器**，用户从 GitHub 下载的唯一入口，放在仓库**最上层**方便直接看到。负责解压便携 Python、复制程序本体、建桌面快捷方式 |
| `README.md` | 中文主说明：功能、原理、9 项方法的出处、赞赏码 |
| `README.en.md` | 英文版说明，内容与 README.md 对应 |
| `CHANGELOG.md` | 更新日志，每个版本新增/修复了什么 |
| `LICENSE` | MIT 开源协议正文 |
| `.gitignore` | 告诉 git 哪些文件不纳入版本控制（构建产物、缓存、日志等）。**不能删** —— 删了会导致 `build*/`、`dist*/`、`__pycache__` 被误提交进仓库 |
| `engines_manifest.json` | **引擎远端清单**。软件「引擎管理 → 检查更新」时拉取它，用于在不重新打包的情况下新增引擎或换模型 |

📌 **这个安装器内部已经包含了 `app/first_run_gui.exe`** —— 打包时通过 `datas=[('app','app')]` 把整个 `app/` 目录塞了进去。所以用户装完之后，磁盘上自然就有引导器，不需要单独下载。

> 💡 安装器原本放在 `dist/` 目录，后来移到根目录，让下载入口更显眼。重新打包时仍会输出到 `dist/`，搬上来即可。

---

## 四、`app/` —— 程序本体

### 4.1 两个入口与打包
| 文件 | 用途 |
|---|---|
| `main.py` | **主程序入口**。用 PySide6 拉起图形界面，最终由便携 Python 运行 |
| `first_run.py` | **首次运行引导器的源码**。检查依赖装没装（PySide6 / torch / transformers 等），缺什么联网装什么，装完自动拉起 `main.py` |
| `first_run_gui.exe`（11.4 MB） | `first_run.py` **打包成的 exe**。为什么要单独打一个？因为安装器装的是官方 embeddable 便携 Python，**天生没有 tkinter**，画不出进度条 —— 只能借 PyInstaller 的壳把它自带的 tkinter 一起打进去 |
| `diag_startup.py` | **一键排障脚本**。把定位问题需要的信息（Python 版本、依赖状态、代理配置、日志尾部）一次性打印出来，供「诊断.cmd」调用 |

> ⚠️ **`app/first_run_gui.exe` 不是冗余文件，不能删。**
> 它是**重新打包安装器时的原料**：删了它，下次打出来的安装器内部就没有引导器，用户装完会缺 PySide6/torch，主程序直接崩。
> （已经打好、放在仓库根目录的那个安装器不受影响 —— 它早把引导器吞进肚子里了。）

### 4.2 `app/core/` —— 核心逻辑（与界面无关）

| 文件 | 用途 |
|---|---|
| `meta.py` | 程序版本号等元信息 |
| `settings.py` | 配置读写：判定阈值、并行数、模型路径、预设存档；也是读取 `engines_manifest.json` 远端地址的地方 |
| `i18n.py` | 中英文界面文案表 |
| `about_text.py` | 「关于」窗口的文案（中英各一份） |
| `license.py` | 免费版判定 + 预留的专业版授权接口（核心功能永久免费） |
| `netfix.py` | **网络自愈**。自动绕开用不了的系统代理（如 socks 代理会让 pip/urllib 直接报错），改成直连 |
| `logging_setup.py` | 日志初始化，日志只记运行信息，**不含论文内容、不上报** |
| `detector.py` | 检测流程编排：切分段落 → 调引擎 → 汇总结果 |
| `cluster.py` | **多设备算力合并**：局域网节点自动发现与任务分发 |
| `diagnosis.py` | 诊断引擎：9 维扫描 + 知网 5 种语言模式，产出段落级 JSON 报告 |
| `therapy.py` | 治疗引擎：三轮降重协议（确定性改写，守住学术语体） |
| `aigc_rules.py` | 降重规则数据：AI 高频词替换表、口语化负面清单、受保护片段规则 |
| `benchmark.py` | 评测基准：RAID / MGTBench 指标计算（准确率、假阳性率、F1） |
| `report.py` | 报告导出（Markdown 等格式） |
| `doc_reader.py` | 读取 PDF / DOCX / TXT 并抽取纯文本 |

### 4.3 `app/core/engines/` —— 可插拔的检测引擎

| 文件 | 用途 |
|---|---|
| `base.py` | 引擎通用基类，规定 `predict_paragraphs()` 等接口 |
| `registry.py` | **插件注册表**。用 `@register("impl")` 装饰器登记实现，启动时自动发现 `engines_plugins/` 里新丢进来的算法文件 |
| `catalog.py` | 内置 9 项方法的清单定义（零依赖，界面和安装器都能安全导入） |
| `manager.py` | 引擎总管：内置清单 + 本地覆盖 + 远端更新 + 用户自定义，四种来源合并 |
| `simpleai_engine.py` | **SimpleAI 中文引擎**（默认）：RoBERTa 序列分类，逐段输出 AI 概率 |
| `perplexity_engine.py` | **GLTR 困惑度引擎**：按困惑度阈值线性插值打分 |
| `curvature_engine.py` | **概率曲率类引擎**：Fast-DetectGPT（自身采样扰动）与 DetectGPT（掩码扰动） |
| `binoculars_engine.py` | **Binoculars**：双模型交叉困惑度比值，免调阈值 |
| `rule_engine.py` | **规则引擎**：用于不需要下载模型的修复/诊断方法 |

> 所有引擎模块都**延迟 import torch** —— 这样即使机器上 torch 被安全软件拦截，引擎清单仍能正常显示。

### 4.4 `app/ui/` —— 图形界面（PySide6）

| 文件 | 用途 |
|---|---|
| `main_window.py` | 主窗口：拖入文件、选引擎、看结果 |
| `engine_dialog.py` | 引擎管理对话框：下载模型、检查远端更新 |
| `benchmark_dialog.py` | 评测基准对话框：导入数据集、看指标 |
| `rewrite_dialog.py` | 降重对话框：展示改写前后对比 |
| `settings_dialog.py` | 设置对话框：阈值、路径、并行数、语言 |
| `glass.py` | 自绘现代工具风 UI 的样式与控件 |
| `assets/*.svg` | 界面用的小图标（勾选、下拉箭头等） |

### 4.5 `app/assets/` —— 静态资源

| 文件 | 用途 |
|---|---|
| `icon.ico` / `icon.png` | 应用图标。`icon.ico` 用于 exe、窗口、任务栏、快捷方式；`icon.png` 用于 README 展示 |
| `donate/alipay.jpg` | 支付宝赞赏码（README 展示用） |
| `donate/wechat_pay.jpg` | 微信赞赏码 |
| `donate/deepseek_usage.png` | 给国际用户的 API 赠送说明配图 |

---

## 五、`installer/` —— 安装器源码

| 文件 | 用途 |
|---|---|
| `installer.py` | **安装器全部逻辑**。做三件事：<br>1. 下载并解压官方 embeddable 便携 Python（多镜像回退）<br>2. 修补 `._pth` 并引导 pip<br>3. 复制程序本体、建桌面快捷方式、写卸载项<br>界面用 tkinter，无需管理员权限、不碰注册表 |

> 装完只是「环境就位」，torch / PySide6 等重依赖由 `app/first_run_gui.exe` 在首次启动时联网安装。

---

## 六、`tools/` —— 开发者自测脚本

这些是**开发时用来验收的**，普通用户不需要运行。

| 文件 | 用途 |
|---|---|
| `test_engines.py` | 引擎框架回归测试（13 项）：**不需要 torch、不需要下载模型**，秒级跑完 |
| `smoke_test.py` | 全功能冒烟测试：不依赖 torch 也能验证主流程可用 |
| `test_detect.py` | 检测引擎离线自测，纯命令行，无界面 |
| `fusion_selftest.py` | 「检测 → 诊断 → 治疗」闭环自检，完全离线 |

运行方式（把 `<解释器>` 换成你的 Python）：

```bash
<解释器> tools/test_engines.py
```

---

## 七、哪些文件不能删

| 文件 | 删了会怎样 |
|---|---|
| `app/first_run_gui.exe` | 下次重新打包时，安装器里就没有引导器 → 用户装完缺依赖 → **打不开软件** |
| `AIGC_Toolkit_Setup.exe` | 用户失去唯一的下载安装入口 |
| `.gitignore` | 构建产物、缓存、日志会被误提交，仓库迅速膨胀 |
| `engines_manifest.json` | 「检查更新」功能失效，无法远端新增引擎/换模型 |
| `app/assets/icon.ico` | 打包时找不到图标 |
| `LICENSE` | MIT 协议声明失效 |

---

## 八、一句话总结

```
用户下载的只有一个文件 →  AIGC_Toolkit_Setup.exe（就在仓库最上层）
它内部装着           →  app/first_run_gui.exe（引导器）+ 程序本体 + 安装逻辑
其余所有文件         →  源码、界面、图标、文档、自测脚本，供开发与维护使用
```

# aigc-toolkit: file purpose marker
