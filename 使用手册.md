# AI 检测工具箱 · 功能使用手册

> 版本 v1.2.8 ｜ 反馈邮箱：gxgx3456@qq.com（遇到 Bug 欢迎邮件说明操作步骤和截图）

---

## 一、安装与首次启动

1. 双击 `AIGC_Toolkit_Setup.exe`，选择安装目录（默认 `D:\AIGC_Detector`），点「开始安装」。
   - 可勾选 **创建桌面快捷方式**（默认勾选）
   - 可勾选 **安装完成后立即运行**（默认勾选）
   - 安装器**自带 Python 3.12 便携运行时**：不需要你电脑上装过 Python，不写系统注册表、
     不需要管理员权限；卸载就是删目录（也会自动清理桌面快捷方式）
   - 下载失败不用慌：安装器会依次尝试 **国内镜像 → 直连 → 系统 curl**，
     还会自动绕开 VPN 那种「socks 系统代理」；全都失败时可手动选择本地已下好的
     `python-3.12.10-embed-amd64.zip`
2. 首次启动会弹出引导窗口，自动下载运行组件和 AI 模型（约 400MB，国内镜像加速，不需要 VPN）。
   - 有 NVIDIA 显卡 → 自动选 CUDA 版；没有 → 自动选 CPU 版
   - 想把模型放别的盘：安装前或装后在「设置 → 下载」里改 **模型保存路径**
3. 窗口右上角三个系统按钮：**— 最小化 ｜ ⛶ 全屏（Esc 退出）｜ ✕ 关闭**
4. 想卸载：Windows **设置 → 应用 → 安装的应用**（或控制面板 → 卸载程序）里找到「AI 检测工具箱」点卸载。卸载前会提示：有 Bug 建议先邮件反馈。

---

## 二、AI 检测（核心功能）

**步骤：**

1. 点 **「选择文件」**，支持 `.txt` / `.docx` / `.pdf`。
2. 在下拉框选检测引擎（首次使用需先下载对应模型，见第九节）：
   - **SimpleAI 中文检测**（约 400MB，推荐，中文论文首选）
   - GLTR 困惑度检测（约 500MB，英文文本较好）
   - **Fast-DetectGPT**（约 5.4GB，零样本，条件概率曲率，建议显卡）
   - **DetectGPT**（约 1.4GB，零样本，掩码扰动，CPU 勉强可跑）
   - **Binoculars**（约 1.9GB，零样本，双模型交叉困惑度，免调阈值）

   点右边的 **「管理」** 可以看到每个引擎的模型清单、体积、论文来源和下载状态。
   不确定就用默认的 SimpleAI。
3. 点 **「开始检测」**，进度条走完出结果。

**结果怎么看：**

- 右侧大数字 = 全文 **AI 率**（所有段落 AI 概率的平均值，0~100%）
- 下方报告逐段列出 AI 概率
- 左侧「诊断」标出高危段落（红色最需要处理）

**检测阈值（滑块）：** 判定"这段是 AI 写的"的概率线，默认 50%。调低 → 更严格（更多段落被判为 AI）；调高 → 更宽松。

---

## 三、降重（手动模式）

检测完成后点 **「降重」** 进入降重窗口：

1. 左侧勾选要改写的段落（默认勾上高危段）。
2. **目标修改率**：被降重处理的段落比例（改写幅度，不是 AI 率达标线）。
3. 选改写强度（轻度 / 标准 / 强力），点 **「开始降重」**。
4. 完成后逐段对照原文和改写稿，可单段还原。
5. 点 **「导出 TXT」** 保存改写后的全文。

原理：本地规则库改写（同义词替换 / 句式重构 / 长句拆分），不联网、不花钱。

---

## 四、自动降重闭环（推荐）

不想手动一轮轮来，用窗口底部的 **「自动降重（循环复检）」**：

1. 设 **目标 AI 率**（默认 ≤ 25%）和 **最大轮数**（默认 3，防死循环）。
2. 点开始，程序自动执行：
   **规则改写 → 本地模型复检全文 AI 率 → 仍超标就只改超标段落 → 循环**，直到达标或轮数用完。
3. 结束后弹窗显示每轮改写段数和 AI 率变化，最终以最后一次复检为准。
4. 段落清单和诊断会自动刷新，确认满意后导出 TXT。

> 注意：自动降重每轮复检要跑一遍本地模型，段落多时每轮需要一点时间，属正常现象。

---

## 五、参数预设

右侧「参数」区可以调：

| 参数 | 作用 |
|---|---|
| 检测阈值 | 判定 AI 的概率线（10–90%） |
| 最短段落长度 | 比这短的段落不参与检测，默认 20 字 |
| 并行工作数 | 多显卡时每个卡一个工作线程 |

- **保存预设 / 加载预设**：把当前参数存成方案反复用
- **导出 / 导入预设**：把方案发给别人或换电脑迁移

---

## 六、局域网集群检测（进阶）

有多台电脑时可以分摊检测量：

- 本机当 **主控**：设置里点「扫描节点」，发现局域网工作节点后，检测会自动把段落分片发给各节点并行算。
- 其他电脑当 **工作节点**：在该电脑上以工作节点模式启动（设置里「启动工作节点」），填主控机的地址。

只有一台电脑不用管这个功能。

---

## 七、设置项速查

| 设置 | 说明 |
|---|---|
| 语言 | 中文 / English 切换 |
| 模型保存路径 | 默认随安装目录，可改到 D 盘等大容量分区 |
| 下载源 | 模型走 hf-mirror 国内镜像，一般不用改 |
| 导出日志 | 遇到问题时把日志发到反馈邮箱，方便定位 |

---

## 八、评测基准（给检测器做体检）

主界面点 **「评测基准」**（或在引擎管理里选中 RAID / MGTBench 后点「运行评测」）。

1. 选检测引擎和判定阈值（默认跟随检测设置）。
2. 选样本：
   - **内置快速体检样本**（15 条中英对照，开箱即用）
   - **导入数据集**：CSV / JSONL / JSON，自动识别 text / label / model 列
     - RAID 官方数据：<https://github.com/liamdugan/raid>
     - MGTBench：<https://github.com/xinleihe/MGTBench>
3. 点 **「开始评测」**，出报告：
   - 准确率、**假阳性率 FPR**（人类文章被判成 AI 的比例，越低越好）、
     假阴性率 FNR、精确率、召回率、F1
   - 按生成模型 / 语言分项
   - 逐条明细（真值 vs 判定）
4. 点 **「导出报告」** 存成 Markdown，可直接贴进论文附录。

> 内置样本是作者手写的快速自检集，**不代表官方基准成绩**；要看真实水平请导入官方数据。

---

## 九、下载模型 / 换模型 / 加新引擎

**下载模型**：左侧「模型下载管理」→ 选下载源（国内镜像 / 官方）→ 点对应引擎的「下载」。
模型只需下载一次，之后完全离线可用；可把保存路径改到大容量磁盘。

**换成别的 HuggingFace 模型**：在安装目录下建 `engines_catalog.json`，例如：

```json
[{"id": "binoculars",
  "models": [{"repo": "tiiuae/falcon-7b"},
             {"repo": "tiiuae/falcon-7b-instruct"}]}]
```

重启软件即生效 —— 只覆盖你写了的那几个字段，其余照旧。

**加一个全新引擎（预留接口）**：把 `.py` 丢进安装目录的 `engines_plugins\`，
里面用 `@register("my_impl")` 声明并实现 `predict_paragraphs()` 即可，不用改主程序。

**拉取远端引擎清单**：引擎管理 →「检查更新」→ 填清单地址（默认已填好项目的
`engines_manifest.json`）。以后作者发布新引擎 / 新模型，不升级软件也能拿到。

---

## 十、常见问题

**1. 双击打开闪退 / 提示"本地 AI 引擎无法加载"？**
原因是某些安全软件的防护驱动拦截了 AI 组件（torch）的加载。已知 **搜狗浏览器的 SCAegis 防护驱动** 会这样。解决办法任选：
- 以管理员身份运行一次程序
- 卸载对应软件，或在其设置里关闭防护驱动，然后重启电脑
- 程序现在即使被拦也能正常打开，只是本地检测会给出提示

**2. 下载模型很慢 / 失败？**
首次启动下载已走国内多镜像 + curl 兜底。仍失败可在设置里检查模型路径所在盘的剩余空间，或换网络重试（不需要 VPN）。

**3. 检测很慢？**
CPU 跑大模型本来就慢，属正常。吃显卡的是 Fast-DetectGPT（5.4GB）和 Binoculars；
DetectGPT 与 GLTR 在 CPU 上也要等，但能跑完。普通论文检测用 SimpleAI 即可。

**4. 改写后 AI 率还是高？**
用「自动降重闭环」多跑几轮；也可手动对高危段落用"强力"强度再改一次。规则改写对模板化句式（"综上所述""首先/其次"）效果最明显。

**5. 检测结果和学校的不一样？**
正常，检测器都受模型和文本类型影响，本工具是自测参考。想看自己这台机器上
某个引擎的真实水平，用「评测基准」导入 RAID / MGTBench 的官方数据子集跑一遍，
报告里的假阳性率最值得看（它衡量的是"把你自己写的判成 AI"的概率）。

**6. 想换模型 / 加自己的检测方法？**
见第九节，三条路都不用改主程序：改清单换模型、丢插件文件加算法、检查更新拉远端清单。

**7. 软件是免费的吗？**
是。如果觉得有用，关于页里有赞赏码，随意支持。
