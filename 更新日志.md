# AI 检测工具箱 · 更新日志

**中文** ｜ English: [CHANGELOG.md](CHANGELOG.md)

> 每个版本改了什么，都记在这里。新版本在最上面。
> 想知道「装出来的软件里能看到什么」，看每一节的**「用户可见变化」**小节。

---

## 📌 本文件的维护规则（每次发版必须照做）

以下是本项目从 2026-09-25 起执行的固定规矩，作者与协作者都要遵守：

1. **本文件 `更新日志.md` 加一节** —— 新版本在最上面，写清「新增 / 修复 / 变更」，并标出**用户可见变化**。
2. **同步英文版 [`CHANGELOG.md`](CHANGELOG.md)** —— 两份内容一一对应，改任一份必须改另一份。
3. **软件内也要有** —— 把新版本的条目加进「关于」窗口的**更新说明**清单
   （源码 `app/core/about_text.py`，中英两段都要加），让用户不联网也能看到这个版本改了什么。
4. **重新打包两个 exe** —— `AIGC_Toolkit_Setup.exe` + `app/first_run_gui.exe`。
   ⚠️ 只改源码不重打包，用户下载安装到的仍是旧内容。
5. **推送 GitHub** —— 以上都做完并自测通过后再推送。

> 为什么定这条规矩：v1.3.1 只改了源码、没重打包，「关于」窗口里的更新说明也就没跟上，
> 用户在软件里看不到任何版本变化；v1.3.2 补打包时才把这笔账还清。
> 从本文件建立之日起，**更新日志同时进 GitHub 和软件内部**，不再有「源码写了、用户看不见」的空档。

---

## v1.3.3（2026-09-25）

> 本版**只改联系方式的展示**，未改动检测 / 降重等任何功能逻辑。

### 用户可见变化
- 软件里多了一个联系方式：**Telegram @A9100010**
  （主界面「⑦ 联系作者」现在同屏显示邮箱与 Telegram，按钮改为「复制联系方式」）
- 国际用户以前要「赠送 AI API Key」才能支持，现在改为**直接 Telegram 联系打赏**，门槛低得多
- 卸载提示、安装器界面、导出日志提示、启动自检提示里，都同时给出邮箱与 Telegram

### 变更：新增 Telegram 联系方式 @A9100010
- 统一联系方式常量（`app/core/meta.py`）：
  `AUTHOR_EMAIL` = gxgx3456@qq.com ｜ `AUTHOR_TG` = @A9100010
  ｜ `AUTHOR_CONTACT` = `gxgx3456@qq.com / Telegram @A9100010`
- **软件内**覆盖点：
  - 主界面「⑦ 联系作者」：同屏显示邮箱与 Telegram，一键复制两者
  - 「关于」窗口横幅下的说明条（`nc_banner_tip`）：商用授权 / 反馈 / 意见 / 合作统一给出两个渠道
  - 「关于」窗口正文：Bug 反馈段、协议段
  - 首启引导器失败提示、启动自检报告提示（`diag_startup.py`）
  - 安装器界面底部、卸载提示、控制面板卸载项的「联系」与「支持链接」
    （`URLInfoAbout` → `https://t.me/A9100010`）
- **仓库文档**覆盖点：`README.md` / `README.en.md` / `使用手册.md` / 本文件 / `CHANGELOG.md`

### 变更：国际用户赞赏说明改为 Telegram 打赏
- 原「赠送 AI API Key」（含 DeepSeek 推荐与型号 / 地址 / 端口清单）整段替换为：
  > If you'd like to tip but don't use either of the two payment methods above,
  > you can contact me on Telegram to send a tip instead - thank you!!!
  > **Telegram: @A9100010** (not my personal account; it is a purchased one)
- 该段在中英两个 README 与软件「关于」窗口中**一律使用英文**
- ⚠️ `app/assets/donate/deepseek_usage.png` 及其仓库说明**保留未删**（本次只是不再引用）

### 检查：软件中英适配全量体检
- 对 `app/core/i18n.py` 做键值对齐检查：中文 313 键 / 英文 313 键，**零缺失、零多余**
- 代码中引用的 i18n 键全部存在，**无悬空引用**
- 英文文案中**零中文残留**（语言切换按钮、品牌名等有意保留项除外）

---

## 仓库文档整理 · 建立中文更新日志（2026-09-25）

> 本次**只调整仓库文档与「关于」窗口的更新说明，未改动程序逻辑** ——
> 安装器与引导器仍是 v1.3.2，**不需要重新下载**。纯文档整理，**不升版本号**。

### 新增：本文件 `更新日志.md`
- 新增中文更新日志（本文件），与英文版 `CHANGELOG.md` 互为对照，两份互加语言链接
- 明确写下上面的「维护规则」：以后每次更新，更新日志要**同时**进
  GitHub（本文件 + `CHANGELOG.md`）和**软件内部**（「关于」窗口的更新说明）

### 补记：v1.3.1 / v1.3.2 的条目补进软件内「更新说明」
- 「关于」窗口的更新说明清单此前只写到 v1.3.0，v1.3.1 与 v1.3.2 两个版本漏了
- 本次补上（源码 `app/core/about_text.py`，中英两段），**下次重打包后用户即可看到**

### 用户可见变化
- 仓库里多了一份中文更新日志，一眼能看到每个版本改了什么
- （软件内补的条目需**下次重打包**才生效；本次安装包未重新构建）

---

## v1.3.2（2026-09-25）

> 本版是 **v1.3.1 的首次实际发布**。v1.3.1 当时只改了源码，从未重新打包 exe，
> 所以用户下载安装到的仍是 v1.3.0 的内容。本版把 v1.3.1 的全部改动连同下述文案清理
> 一起打进安装包。

### 用户可见变化
- **升级不再清空自己的设置和模型**：主题、阈值、参数预设原样保留；
  几个 GB 的模型不再被删掉重下
- **装过 VPN / 代理软件又卸载过的电脑，以前 100% 装不上，现在能装了**
- **安装包里不再夹带作者本机文件**（设备标识、运行日志）
- 安装包体积：23.59 MB → **22.13 MB**

### 变更：移除「免责声明」段落
- 删除「关于」窗口与项目文档中的整段免责声明（「本人还是一名学生，代码可能存在不足，
  不好勿喷，欢迎友善的建议与改进」），共 4 处：
  `app/core/about_text.py`（中文段 `【免责声明】` + 英文段 `[Disclaimer]`）、
  `README.md`、`README.en.md`
- 同步清理「支持与赞赏」段中同类的自我描述（中英文各一处）
- **保留**「检测结果受模型与文本类型影响，仅供自测参考，请以学校 / 期刊官方认定为准」
  这一功能性声明 —— 它与作者个人情况无关，属于必须告知使用者的使用提示

### 修复：一类环境下安装器 100% 装不上（畸形残留代理）
- **现象**：便携 Python 下载成功，却卡在初始化 pip 并失败：
  `ERROR: Could not install packages due to an OSError: Please check proxy URL.
   It is malformed and could be missing the host.`
- **根因**：代理软件卸载后，注册表 `HKCU\...\Internet Settings` 可能残留
  `http` / `https` / `ftp` 三个值为 `http://`（**有协议头、没有主机名**）。
  `urllib.request.getproxies()` 会把它们读出来交给 pip，pip 解析不了，连接建不起来。
  而 `app/core/netfix.py` 的自愈逻辑只识别 `socks://` 前缀，把 `http://` 判成
  「正常代理，不用管」→ 自愈完全不触发。
- **修复**：`is_unusable_proxy()` 增加「有协议头但没有主机名」的判定；
  `unusable_system_proxy()` 改为直接看 `urllib.request.getproxies()`
  （它是 urllib / requests / pip 的共同入口，同时覆盖环境变量与注册表），
  不再只看注册表的 `ProxyEnable` —— 该值可能已是 0，残留值却照样被读出来。
  命中后走原有路径：`NO_PROXY=*` + 清不可用代理变量，主进程与 pip 子进程一起直连。
- **影响面**：不只在安装器 —— 主程序下载模型走的是同一套逻辑，
  所以「模型下不动」也可能是这个原因。装过 VPN / 代理软件又卸载过的机器建议用本版。
- 验证：`is_unusable_proxy()` 12 条边界用例全过（socks 系、`http://`、`http://:8080`、
  正常代理、带用户名密码的代理等）；实测修复后 `getproxies()` 由
  `{'http': 'http://', 'https': 'http://', 'ftp': 'http://'}` 变为 `{'no': '*'}`，
  子进程拿到的也是干净环境。

### 修复：覆盖安装会重置用户设置
- `installer/installer.py` 收尾处原本**无条件覆盖写** `<安装目录>/settings.json`，
  且只写 `install_dir` 与 `language` 两个键 —— 用户调过的主题、阈值、引擎与参数预设
  每次升级都被清空。
- 改为「读旧值 → 只更新这两个键 → 写回」，其余键原样保留。
- 顺带删掉一个定义了却从未使用的 `shutil.ignore_patterns(...)` 变量。

### 清理：安装包不再夹带作者本机文件
- `AIGC_Toolkit_Setup.spec` 的 `datas=[('app','app')]` 会把 `app/` 整个打进安装包，
  于是每次构建都夹带了：
  - `app/settings.json` —— 含**作者本机的设备标识**（`device_id`）；且程序实际读的是
    `<安装目录>/settings.json`（见 `app/main.py`），这个文件本就是历史遗留，纯属泄露
  - `app/logs/` —— 作者本机的运行日志与诊断输出
  - `app/__pycache__/` —— 多个 Python 版本的 `.pyc`，其中还留着**旧版文案的编译产物**
- 打包前把这些内容移出 `app/`；`diag_startup.py` 的「安装内容」检查项也改为
  检查安装根目录的 `settings.json`（原先检查 `app/` 下的，排除后会误报缺失）。
- 体积变化：安装器 23.59 MB → 21.47 MB，引导器 11.38 MB → 10.35 MB。

### 重新打包两个 exe（v1.3.1 的改动自此生效）
- `AIGC_Toolkit_Setup.exe`（安装器）与 `app/first_run_gui.exe`（首启引导器）均已重新构建
- 此前只在源码里的改动，从本版起真正体现在用户装出来的软件中：
  ① 移除「开发声明」；② 软件内 3 处「禁止商业使用」标注；
  ③ 覆盖安装不丢用户数据、也不再重置设置；④ 残留畸形代理的机器也能装上
- 版本号同步 4 处：`installer/installer.py:APP_VER`、`app/core/meta.py:APP_VERSION`、
  本文件、`使用手册.md`

---

## v1.3.1（2026-09-24）

> 本版两项改动都是**面向使用者体验**的：协议改成禁止商用，安装器支持覆盖安装不丢数据。
> ⚠️ 本版只改了源码，**未重新打包 exe** —— 用户直到 v1.3.2 才真正用上这些改动。

### 用户可见变化
- 软件内 3 处可见「禁止商业使用」标注；协议由 MIT 换成禁止商用
- 安装器支持覆盖安装，升级不再丢用户数据

### 变更：协议改为禁止商业使用
- `LICENSE` 由 **MIT** 换成 **PolyForm Noncommercial License 1.0.0**。
  MIT 明确允许他人商业使用甚至转售，与「禁止商用」互斥，必须换掉才有效力。
- `README.md` / `README.en.md` 的协议段落重写：列出「✅ 允许」与「❌ 禁止」两份清单、
  商用授权联系方式；三处「免费开源」措辞改为「个人免费」/「源码公开，禁止商用」
  （严格讲，限制使用领域的协议不属于 OSI 认可的开源协议）
- `app/core/about_text.py`（中英各一份）「关于」窗口文案同步更新
- **软件内新增 3 处可见标注**：
  ① 主窗口标题栏下方常驻一行 `© 2026 gxgx3456 · 禁止商业使用`；
  ② 「关于」窗口顶部红色醒目横幅 + 橙色说明条；
  ③ 「关于」窗口正文的协议段落（含允许 / 禁止清单与授权邮箱）
- `仓库文件说明.md` / `REPO_FILES.md` 中 `LICENSE` 条目同步更新

### 新增：安装器支持覆盖安装（不丢用户数据）
- **旧行为**：安装时 `shutil.rmtree(appdir)` 把整个 `app/` 删掉再复制新版本。
  副作用有三：① 用户的 `logs/` 被清空，升级后出问题查不到旧日志；
  ② 若用户在设置里没改模型路径，模型默认落在 `<安装目录>/models`，
  会随 `app/` 一起被删 —— 几个 GB 要重下；③ 软件正在运行时 `app.log` 被占用，
  `rmtree` 直接失败，导致「在正在运行的软件上重装」装不上。
- **新行为**：改为**按文件覆盖** —— 新文件写入、旧版本已移除的文件清理掉，
  但 `logs/`、`models/`、`cache/`、`config/` 四个目录整体保留，不被覆盖也不被清理。
  单个文件被占用时记录日志并跳过，不中断整个安装。
- 安装日志会打印一行统计：`覆盖安装完成：新增 N，更新 N，清理旧文件 N，保留 N`
- 运行时 Python（`runtime/python`）原本就带复用逻辑，覆盖安装不会重下

### 验证
- 覆盖安装逻辑用模拟目录做了 10 项断言测试（新增 / 更新 / 清理旧文件 /
  日志保留 / 日志内容不被覆盖 / 模型目录保留 / 模型文件完好），**10/10 通过**
- 改动文件均通过语法检查

---

## 移除开发声明（2026-09-23）

> 本次**只改文案，未改动任何程序逻辑** —— 安装器与引导器仍是 v1.3.0。

- 移除项目文档与「关于」窗口中的「开发声明」段落，共 4 处：
  `README.md` / `README.en.md` / `app/core/about_text.py`（中文段 + 英文段）
- 该段落原文公开了作者使用的 AI 辅助工具链，属作者个人开发细节，不再对外展示
- 顺手修正英文免责声明中一处遗留断行（`The author is still a student;` 前次改动后单独成行）

---

## 仓库整理（2026-09-23）

> 本次**只调整仓库内容，未改动任何程序代码** —— 安装器与引导器仍是 v1.3.0，无需重新下载。

- 移除 5 个仅供作者本机使用的开发脚本（`tools/` 下的 `download_simpleai_model.py`、
  `run_detection_test.bat`、`make_icon.py`、`grab_screen.py`、`patch_offline_fix.py`）：
  它们写死了开发机的绝对路径，对使用者没有价值；模型与依赖的获取由首启引导器负责
- 移除 `docs/` 下 4 个与 `app/assets/` **逐字节重复**的图片，README 直接引用 `app/assets/`，
  省去一份冗余
- 保留 `tools/` 下的测试脚本：`test_engines.py`、`smoke_test.py`、`test_detect.py`、`fusion_selftest.py`
- 仓库回到「源码 + exe + 文档 + 测试」四类内容，无开发机痕迹

---

## v1.3.0（2026-09-19）

> 本版是**第一次把 v1.2.8 的「九引擎」真正打包发出去** —— v1.2.8 当时只存在于源码，
> 安装器 exe 还停在 v1.2.7。现在安装器与引导器都已重新打包，装完即是新版。

### 用户可见变化
- 全新应用图标（窗口、任务栏、安装器、桌面快捷方式、卸载列表统一）
- 小屏（1440×900）全屏时按钮文字不再被裁、不再出现「花字」

### 新增：全新应用图标
- 程序窗口、任务栏、安装器、首启引导器、桌面快捷方式、
  「设置 > 应用」卸载列表 —— 全部换成新图标（绿色圆角方块 + 放大镜 + 二进制文档）
- 多尺寸 `.ico`（16 / 24 / 32 / 48 / 64 / 128 / 256），高分屏与任务栏小图标都不糊
- **透明底、无白框**：圆角之外是透明的，浅色 / 深色主题下都不会出现「白底方块」；
  原图右下角的生成水印与投影已裁掉
- 任务栏归组修正：显式声明进程身份，窗口不再被算成 `pythonw.exe`（原先任务栏图标是 Python 的）

### 修复：全屏时按钮文字被裁（「花字」）
- 根因（实测）：左侧面板最小高度约 1082px，而 1440×900 屏幕上全屏只有 900px，
  Qt 把所有控件**等比压扁**，按钮从 34px 压到 21px，文字需 34px 就被裁掉。
  普通窗口时 Qt 偷偷把窗口撑到 1082（底部超出屏幕）所以看不出来
- 左面板改为**滚动**而不是压扁；按钮加最小高度锁定；
  新增统一的「窗口不超出屏幕」工具，主窗口 / 引擎管理 / 下载设置 / 降重 / 关于 全部套用
- 顺带修掉下载设置里「浏览…/删除」按钮被 `setFixedWidth(70/60)` 写死导致的裁字
- 回归：全部窗口被压扁的控件数 **19 → 0**（普通窗口与全屏都是 0）

### 含 v1.2.8 的全部内容
9 个学术方法全部落地为可运行模块，分「检查 / 修复 / 评测」三类，
模型一律按需下载、软件不预置，并预留插件与远端清单扩展接口 —— 详见下方 v1.2.8 条目。

---

## v1.2.8（2026-09-19）

### 新增：9 个引擎全部落地，分「检查 / 修复 / 评测」三类
以前 README / 关于里列的 9 项学术方法，只有 3 项真跑得起来，其余是纸面引用。
这一版全部做成可运行的东西：

**检查引擎（5 个，判断文本是否 AI 生成）**
- `SimpleAI / HC3`（默认中文引擎）—— 原有
- `GLTR` 困惑度 —— 原有
- `Fast-DetectGPT` —— **重写为论文的条件概率曲率**（采样近似），
  不再是「困惑度换个模型」的挂名实现
- `DetectGPT` —— **新增**，论文的掩码扰动实现：T5 补全随机挖掉的片段，
  比较对数概率曲率
- `Binoculars` —— **新增**，同词表双模型交叉困惑度比，免调阈值

**修复引擎（2 个，诊断 + 降重，内置规则不需要下载模型）**
- `aigc-reduce` 三轮降重协议、知网 5 种语言模式诊断 —— 原有，现在在引擎管理里可见可选

**评测基准（2 个，给检测器做体检）**
- `RAID`、`MGTBench` —— **新增可运行模块**：用带标注样本跑出准确率、
  假阳性率（人类文章被判成 AI 的比例）、假阴性率、精确率/召回率/F1，
  并按生成模型与语言分项；支持导入官方数据集子集（CSV / JSONL），
  可导出 Markdown 报告

### 新增：模型一律按需下载，软件不预置
和以前一样：清单里只声明「用哪个模型、多大、去哪下」，用户在「下载设置」
里点下载。默认中文场景只需 SimpleAI 一个（约 400MB）；
GLTR/Binoculars 约 0.5~1.9GB；Fast-DetectGPT 约 5.4GB。

### 新增：预留扩展接口（以后加模型不用重新打包）
1. **引擎插件目录** `engines_plugins/*.py` —— 丢一个文件进去，
   里面用 `@register("my_impl")` 声明，启动时自动发现；
2. **引擎清单可远端更新** —— 保存一份 `engines_manifest.json` 到任意可访问地址，
   在「引擎管理 → 检查更新」里填写 URL 即可拉到新引擎 / 新模型条目；
3. **模型由清单声明** —— 每个引擎用哪个 HuggingFace 模型写在清单的 `models` 里，
   换模型（比如把 Binoculars 换成 Falcon 组合）改清单即可，不必动代码。

### 界面
- 引擎管理重做：左侧按「检查 / 修复 / 评测」三类分组，右侧显示模型清单、
  体积、论文来源、本地是否已下载；内置规则引擎标注「内置可用」
- 检测引擎下拉只列检查类，不再混入修复 / 评测条目
- 新增「评测基准」入口（主界面 + 引擎管理）
- 下载设置里的模型列表改为按引擎展示，并显示下载状态

### 杂项
- 修正关于窗口里的失实描述：删掉「匿名遥测（可关闭）」（本项目没有遥测），
  技术架构段如实列出 5 个引擎与两类基准
- 新增回归测试 `tools/test_engines.py`（13 项：清单完整性、分类、清单覆盖、
  插件自动发现、模型解析、评测指标、样本导入、修复引擎可用性）

---

## v1.2.7（2026-09-19）

### 修复：安装失败的根因 —— 官方 Python 安装器的「MSI 孤儿注册」
- 安装器**不再调用官方 .exe 安装器**，改用官方 **embeddable 便携包**
  (`python-3.12.10-embed-amd64.zip`)：下载 → 解压到 `runtime\python` → 修补 `._pth`
  → 引导 pip。不写注册表、不需要管理员权限、卸载=删目录，
  彻底告别「静默安装退出码 0 却什么都没装上」以及「卸载/修复全 1603」
- 安装前自动清理不完整的旧运行时与历史遗留的 `venv`

### 修复：下载失败的根因 —— socks 系统代理
- 新增**网络自愈**（源码侧 `app/core/netfix.py` + 便携运行时里的 `sitecustomize.py`）：
  VPN / 加速器常把 Windows 系统代理写成 `socks://127.0.0.1:xx`，而 Python 的
  urllib / requests / pip 都不支持 socks —— 表现为 `BadStatusLine` 或
  `Missing dependencies for SOCKS support`，下载全挂（curl 却正常）。现在检测到这类
  代理会自动改走直连
- 下载链路升级为三段兜底：**urllib → 直连 urllib（绕开系统代理）→ 系统 curl**，
  并在下载后做体积 + 文件头（`PK`）双重校验，杜绝把错误页当安装包

### 修复
- 首次启动引导器改用**运行时 Python** 执行 pip（此前打包成 exe 后 `sys.executable`
  指向 exe 自身，pip 根本跑不起来）
- 桌面快捷方式 / 启动命令**直接指向 `first_run_gui.exe`**（此前用 pythonw 去「运行」
  一个 exe，等于把 exe 当脚本喂给解释器，必错）
- 引导器打包成 exe 却找不到 `runtime\python` 时给出明确提示，而不是继续硬跑

### 优化
- 安装逻辑全部抽成模块级函数，GUI 只是外壳；新增**无界面安装**：
  `AIGC_Toolkit_Setup.exe --cli <目标目录>`（便于批量部署与自测）
- 启动脚本、错误提示、手动选包对话框同步改为便携包说法

---

## v1.2.6（2026-09-19）

### 修复
- **HiDPI 缩放下的文字重影 / 花字**：去掉半透明窗口与阴影效果（分数缩放下会渲染错位），
  显式指定「微软雅黑 UI」字体

### 优化
- 按设计规范重做界面：发丝边框、单一强调色（#2563eb 占比 <5%）、中性灰底、
  圆角 ≤16px、细进度条，替换原玻璃拟态渐变风格

---

## v1.2.5（2026-09-18）

### 修复
- **打开闪退**：`torch` 被安全软件（实测为搜狗浏览器驱动 `SCAegis64.sys`）拦截加载时，
  改为容错启动并给出友好提示，不再直接崩溃

---

## v1.2.4（2026-09-18）

### 新增
- **控制面板卸载入口**：安装后可在 Windows「设置 > 应用」或控制面板「卸载程序」中卸载本软件
  - 卸载前会列出将删除的目录，并提示遇到 Bug 可先联系反馈（gxgx3456@qq.com / Telegram @A9100010）
  - 卸载时自动清理注册表卸载键、桌面快捷方式与整个安装目录（含模型文件）
- **安装选项可勾选**（均默认开启）：
  - 创建桌面快捷方式
  - 安装完成后立即运行（自动拉起首次启动引导）
- 安装器**全屏按钮移至右上角**（系统按钮风格，Esc 退出）

### 优化
- 「关于」页面补充 v1.2.3 / v1.2.4 更新说明（中英双语）

---

## v1.2.3（2026-09-18）

### 新增
- **自动降重闭环**（此前为半自动，只改一轮）：
  - 降重窗口新增「自动降重（循环复检）」按钮
  - 每轮：规则改写 → 本地模型复检全文 AI 率 → 仍超标则只对超标段落再改
  - 循环直到 AI 率达到目标值（可设，默认 ≤25%）或最大轮数（可设，默认 3，防无限循环）
  - 完成后弹窗汇报每轮改写段数、复检 AI 率与最终 AI 率，段落清单/诊断同步刷新

### 优化（安装器下载方案，解决部分用户下载失败）
- 下载源扩为 **npmmirror → 华为云 → python.org** 多镜像依次尝试（国内镜像无需 VPN）
- 每个源：Python 自带下载失败自动切换**系统 curl** 兜底（独立网络栈）
- 下载完成后体积校验（<5MB 判为坏包，自动换源重下）
- 所有源均失败时，可**手动选择本地已下载的安装包**，完全不依赖网络

---

## v1.2.2（2026-09-13）

### 修复
- Python 静默安装「退出码 0 却失败」：官方安装器被旧注册表记录干扰时，
  改为从注册表/默认位置找回真实安装目录并复制；仍失败则用可见模式重试一次
- 安装器新增**左上角全屏按钮**（查看长日志更方便，Esc 退出）
- 首次启动引导器增加文件日志（`app/logs/first_run.log`）与主程序 stderr 捕获

---

## v1.2.1 / v1.2.0（2026-09 上旬）

- **轻量安装器**：AI 组件（PyTorch/模型）改为首次启动时自动下载，CPU/CUDA 自动选择
- **模型保存路径可自定义**（可放 D 盘），检测/降重引擎统一按配置解析模型目录
- 首次启动引导器 `first_run.py`：进度条下载 + 完成后自动启动主程序
- 内置国内镜像加速（HF 镜像 / 清华 PyPI / npmmirror）
- 模型路径、降重目标等设置项支持随预设存档

---

## 反馈

遇到 Bug，或有建议、意见、合作意向，欢迎联系（两个渠道都可以）：

- 邮箱：**gxgx3456@qq.com**
- Telegram：**@A9100010**
